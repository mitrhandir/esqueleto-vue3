import { ActionTree } from "vuex";
import { RootState } from "../types";
import { AlmacenState } from "./types";
import { AlmacenMutation } from './mutations';

export const actions: ActionTree<AlmacenState, RootState> = {
  
}